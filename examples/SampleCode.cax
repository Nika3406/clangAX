#import "_math"

func() = "test"
{
	x = 8
	y = 9
	z = x + y
	print(z)
	print("\n")
}

func() = "ArrayTest"
{
	a = [1, 2, 3, 4, 5]
	b = [5, 6, 7, 8, 9]
	for (idx = 0; idx < len(a); idx++)
	{
		print(a[idx])
		print("\n")
	}
}

func(Main)
{
	print("---Test Function---\n")
	test()
	print("---Array Test Function---\n")
	ArrayTest()
}
